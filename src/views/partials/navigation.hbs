{{!-- Navbar --}}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/">
            <i class="fas fa-music"></i>
            Spiller Music
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            {{!-- Apartado de canciones el cual se visualiza independientemente de si es o no usuario--}}
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/tracks">Canciones</a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                {{!-- Si es usuario se visualizaran las siguientes opciones--}}
                {{#if user}}
                {{!-- Apartado de playlist --}}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        Playlists
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        {{!-- Ver playlist --}}
                        <a class="dropdown-item" href="/profile/playlists">Mis playlist</a>
                        <div class="dropdown-divider"></div>
                        {{!-- Crear playlist --}}
                        <a class="dropdown-item" href="/profile/playlists/new">Nueva playlist</a>
                    </div>
                </li>
                {{!-- Apartado de usuario --}}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        {{user.username}}
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        {{!-- Ir al perfil del usuario --}}
                        <li><a class="dropdown-item" href="/profile">Profile</a></li>
                        {{!-- Cerrar sesion --}}
                        <li><a class="dropdown-item" href="/login/logout">Logout</a></li>
                    </ul>
                </li>
                {{!-- Si no es usuario se visualizaran las siguientes opciones --}}
                {{else}}
                {{!-- Login --}}
                <li class="nav-item">
                    <a class="nav-link" href="/login">Iniciar sesi&oacute;n</a>
                </li>
                {{!-- Register --}}
                <li class="nav-item">
                    <a class="nav-link" href="/signup">Registrarse</a>
                </li>
                {{/if}}
            </ul>
            {{!-- Barra de busqueda --}}
            <form class="d-flex" method="post" action="/search" role="search">
                <input class="form-control me-2" type="search" name="query" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-info" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>